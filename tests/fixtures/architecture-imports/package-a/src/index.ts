import { secretValue } from "../../package-b/src/internal/secret";

export const leakedSecretValue = secretValue;

export const flatEntries = snapshot.manifest.entries;
export const flatReader = new FlatNamespaceReader();
export const snapshotPacks = objectManifests.packObjectKeys;
export const snapshotPackPointers = objectManifest.packObjects;
export const cachedBoundary = manifestChunkCache;
